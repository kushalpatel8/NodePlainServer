A simple, lightweight HTTP server built using the native Node.js `http` module. This project demonstrates basic routing and environment configuration without relying on heavy frameworks like Express.

## 🚀 Features

* **Native HTTP Server**: Built using Node's core `http` module.
* **Basic Routing**: Handles routes for Home, About, and Contact pages.
* **Environment Configuration**: Uses `dotenv` to manage configuration like port numbers.
* **Dev Mode**: Includes `nodemon` for hot-reloading during development.

## 🛠️ Tech Stack

* **Runtime**: Node.js
* **Dependencies**: `dotenv`
* **Dev Dependencies**: `nodemon`
